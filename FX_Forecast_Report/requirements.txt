# FX Forecast Report — dependencies
# NOTE: This project deliberately does NOT use the Bigdata Python SDKs
# (bigdata-client / bigdata-research-tools). All Bigdata.com data is pulled
# from the remote MCP server (https://mcp.bigdata.com/) via the standard
# `mcp` client over streamable HTTP.

mcp>=1.9.0
openai>=1.40.0
python-dotenv>=1.0.1
pandas>=2.2.0
nest-asyncio>=1.6.0
pydantic>=2.7.0
ipython>=8.18.0
jupyterlab>=4.2.0
tqdm>=4.66.0
