# Install pipeline dependencies from the sibling CLI package.
# From Thematic_Screener/:  pip install -r requirements.txt
# Recommended: cd ../Thematic_Screener_CLI && uv sync --group jupyter
-r ../Thematic_Screener_CLI/requirements.txt
jupyterlab>=4.0.0
matplotlib>=3.0.0
